# Azure OpenAI Configuration
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/
AZURE_OPENAI_API_KEY=your-api-key-goes-here
AZURE_OPENAI_API_VERSION=2025-05-01

# Model Deployments
# Names of your model deployments in Azure OpenAI
# AZURE_OPENAI_CHAT_DEPLOYMENT  – primary model for the Dungeon Master agent (e.g. gpt-4o)
# AZURE_OPENAI_MINI_DEPLOYMENT  – cheaper model for structured tasks such as
#                                  scene narration (e.g. gpt-4o-mini, ~94 % cheaper)
#                                  Falls back to AZURE_OPENAI_CHAT_DEPLOYMENT when unset.
AZURE_OPENAI_CHAT_DEPLOYMENT=gpt-4o-mini
AZURE_OPENAI_MINI_DEPLOYMENT=gpt-4o-mini
AZURE_OPENAI_EMBEDDING_DEPLOYMENT=text-embedding-ada-002
AZURE_OPENAI_DALLE_DEPLOYMENT=gpt-image-1-mini

# Azure AI Foundry project endpoint
# Format: https://<account>.services.ai.azure.com/api/projects/<project>
AZURE_AI_PROJECT_ENDPOINT=https://your-account.services.ai.azure.com/api/projects/your-project

# Cost controls – image generation budget
# DALL-E images are 71-83 % of Azure OpenAI spend. These settings cap the
# number of images a single session may generate within a rolling time window.
# Set MAX_IMAGES_PER_SESSION=0 to disable the cap entirely.
MAX_IMAGES_PER_SESSION=3
IMAGE_SESSION_WINDOW_MINUTES=30

# Application Settings
APP_HOST=0.0.0.0
APP_PORT=8000
APP_DEBUG=True
APP_LOG_LEVEL=info

# CORS Settings (comma-separated list of allowed origins)
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Storage Settings
STORAGE_CONNECTION_STRING=your-storage-connection-string-here
